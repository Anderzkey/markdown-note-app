# JavaScript Tips

Learn useful JavaScript patterns and best practices.

## Arrow Functions

Arrow functions provide a concise syntax:

```javascript
const add = (a, b) => a + b;
const double = x => x * 2;
```

## Destructuring

Extract values from objects and arrays:

```javascript
const { name, age } = user;
const [first, second] = array;
```
